@model IEnumerable<ExamAndQuiz.Models.Grade>

@{
    ViewBag.Title = "Siniflər";
    Layout = "~/Views/Shared/_LayoutBack.cshtml";
}

<h2>Siniflər</h2>

<p>
    <a href="/Grade/Create" class="btn btn-outline-dark">Yeni Sinif</a>
</p>
<table class="table table-bordered table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Sinif)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Sinif)
        </td>
        <td style="width:100px; text-align:center">
            <a href="/Grade/Edit/@item.ID"><i class="fa fa-edit" style="color:green"></i></a> |
            <a href="/Grade/Delete/@item.ID"><i class="fa fa-times-circle" style="color:red"></i></a>
        </td>
    </tr>
}

</table>
